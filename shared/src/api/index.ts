// Mini-app-only API surface: the typed client + TanStack Query hooks generated
// by `pnpm gen:api` (orval) from the backend OpenAPI spec, plus the initData
// auth holder the fetcher reads.
//
// `./generated.ts` is generated — run `pnpm gen:api` (against the live or a local
// backend) before building. Never hand-write response types (docs/contracts.md).
export * from './generated';
export * from './auth';

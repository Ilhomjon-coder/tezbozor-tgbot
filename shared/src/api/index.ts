// Mini-app-only API surface: the typed client + TanStack Query hooks generated
// by `pnpm gen:api` (orval) from the backend OpenAPI spec.
//
// `./generated.ts` is generated — run `pnpm gen:api` (with the backend running)
// before building. Never hand-write response types (docs/contracts.md).
export * from './generated';
